#include<stdio.h>

int main(int argc, char const *argv[])
{
    int a = 10, b = 20;
    //double a = 0.1, b = (double)1/10;


    //0 tương đương với false
    //1 tương đương với true
    printf("%d\n", a == b);
    printf("%d\n", a != b);

    //Thực hiện tương tự với các toán tử >, <, >=, <=
    return 0;
}
