#include<stdio.h>

int main(int argc, char const *argv[])
{
    /* Khai báo mảng */

    //Khai báo mảng số nguyên tên là arr và có 10 phần tử
    int arr[10];

    //Gán giá trị cho phần tử của mảng.
    //Truy cập vào phần tử thông qua index của phần tử đó.
    // arr[0] = 2;
    // arr[1] = 4;
    // arr[2] = 6;
    // arr[3] = 8;
    // arr[4] = 10;
    // arr[5] = 12;
    // arr[6] = 14;
    // arr[7] = 16;
    // arr[8] = 18;
    // arr[9] = 20;

    //Nhập giá trị cho phần tử trong mảng, sử dụng vòng lặp for
    // for (int i = 0; i < 10; i++)
    // {
    //     printf("arr[%d] = ", i);
    //     scanf("%d", &arr[i]);
    // }
    

    //Lấy và in ra giá trị tương ứng với index của phần tử trong mảng
    // printf("arr[%d] = %d\n", 0, arr[0]);
    // printf("arr[%d] = %d\n", 1, arr[1]);
    // printf("arr[%d] = %d\n", 2, arr[2]);
    // printf("arr[%d] = %d\n", 3, arr[3]);
    // printf("arr[%d] = %d\n", 4, arr[4]);

    // for (int i = 0; i < 10; i++)
    // {
    //     printf("arr[%d] = %d\n", i, arr[i]);
    // }
    

    
    
    
    
    return 0;
}
