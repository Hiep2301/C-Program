#include<stdio.h>

int main(int argc, char const *argv[])
{
    // int a = 15, b = 4;
    // double c = (double)a / b;

    // printf("%d / %d = %.2lf\n", a, b, c);

    int a = 1;
    char b = 'a';

    printf("%d", a + b);
    return 0;
}
